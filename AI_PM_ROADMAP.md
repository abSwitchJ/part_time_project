# AI PM 实习作品集路线图

**前置阅读**:`AI_PM_DIAGNOSIS.md`(诊断报告)。这份路线图是诊断结论的"反面"——每条短板都对应一条补救项。

**排序逻辑**:**ROI = 对实习简历/面试的边际收益 ÷ 投入工作量**。优先做 P0,这两项不补,投出去的简历回报率会被压制。

**适用阶段假设**:大三在校,每周可投入作品的时间约 8–15 小时;目标在 1–2 个月内把这份作品打磨到"敢主动甩链接给招聘方看"的程度。

---

## P0 —— 必补,补完再投简历

### P0-1 · AI 匹配 + 排序模块的产品规格

> ✅ **2026-05-16 已完成,定位为 v2 愿景(非 MVP)**:小程序 v1 尚未做起来,"正式接入大模型"延后。已交付 `AI_MATCHING_SPEC.md`(顶部显著标注 v2/非 MVP)+ PRD §6.3「匹配引擎口径说明」+ §8 v2 表后指引段 + **PRD §3.5「AI 辅助筛选」**(按用户同日第二次指令新增,标题带 `v2 · 本版不做` 徽章 + 开头加粗范围标记,确保不被误读为 MVP 已实现)。

**对应短板**:`AI_PM_DIAGNOSIS.md` §1.2 AI 含量、§1.5 第四个故事的空洞。

**为什么是 P0**:这是把"AI PM 候选人"四个字坐实的核心交付物。简历投递之后,招聘方第一关是关键词扫描("匹配 / 排序 / 推荐 / 模型 / 评估指标 / AB"),目前作品里这些词出现密度极低;面试官第一个问题大概率是"AI 在哪",目前最好的回答只能撑 1 分钟。

**交付物**:

1. **在 PRD 里新增 §3.5 设计决策**:"为什么 v3 引入 AI 辅助筛选(而不是替代中介)"
   - 用现有 §3 三段式格式(决策 / 取舍 / 实现位置)
   - 核心叙事:**人机协同**而非取代——AI 做 top-K 初筛(N×3 个候选 → N×1 候选池),中介方做最终 rank 决策 + 解释。这样 AI 失败成本可控、训练数据来源清晰、招聘方对 rank 的信任锚点不变。

2. **新建独立文档 `AI_MATCHING_SPEC.md`**:正经的 AI 模块产品规格,至少包含:
   - **介入点**:在主流程哪个消息节点之前/之后跑(建议:#15–#17 之间,中介方打开报名池时,AI 先把池子按相关性排序 + 给出 top-K 推荐)
   - **输入特征**:应聘方画像(学历、技能 tag、历史成单类别 / 评分 / 准时率)、招聘需求(类别、时段、地点、预算、必要技能)、上下文(报名时间、距截止时间、招聘方历史偏好)
   - **输出形态**:rank score(0–1) + 置信度 bucket(高/中/低) + 推荐理由(2–3 条可读 tag,如"历史同类兼职 4 单全好评")
   - **置信度处理 + 兜底策略**:高置信直接置顶 + 高亮;低置信沉到底部但保留;系统失败时降级为按报名时间排序
   - **冷启动方案**:从规则到模型的演进——v3.0 用规则打分(类别完全匹配 +50 / 时段重叠 +30 / 历史好评 +20),v3.1 在累积 1 万条筛选样本后切 LR/GBDT,v3.2 引入轻量向量召回。**这一段是面试 talking point 的金矿**——展现"会按数据成熟度选模型,不上来就 LLM"。
   - **评估指标**:中介方 top-3 接受率(AI 推的 top 3 里被中介保留几个)、招聘方对 AI-辅助 rank 的确认通过率、应聘方应答率(对比无 AI 基线)
   - **AB 实验设计**:对照组(纯人工)/ 实验组(AI 排序后人工复核),分流单元为招募令,样本量目标 200 单/组,主指标"中介筛选耗时"&"招聘方一次性确认率"
   - **可解释性 / 失败模式**:列出 3 类已知风险(冷启动期数据稀疏、长尾类别覆盖不足、招聘方个人偏好难学到),每类给"AI PM 怎么应对"的产品策略

3. **同步更新**:把 §6.2 表格里"匹配引擎"那两条(无匹配兼职 / 临近截止 24h)展开成 1 个小节,引用 `AI_MATCHING_SPEC.md`。

**投入估计**:8–12 小时(主要在 `AI_MATCHING_SPEC.md`)。

**面试转化**:讲完后能撑住至少 15 分钟深入追问;招聘方简历筛选阶段命中关键词显著提升。

---

### P0-2 · 指标体系 / 北极星指标

> ✅ **2026-05-17 已完成**:交付 `METRICS.md`——**指标设计/验证假设体系**(非数据报告,产品未上线、零实测数据,阈值全部标注"假设·待验证")。北极星 = **双确认成单率**(发布→已成单且 7 日内未取消的占比,抗刷量);§3.1–§3.4 每条决策配 1–2 个验证指标 + 假设阈值 + 数据来源;含护栏指标(应聘方 NPS / 客服工单率 / 中介 #17 耗时)与 §4「证伪逻辑」(什么结果出现就推翻该设计、下一步怎么改)。状态名/编号严格对齐 PRD §3/§5/§7;埋点字段级规格留待 P1-3。

**对应短板**:`AI_PM_DIAGNOSIS.md` §1.1 产品深度的"弱项 / 无数据思考"。

**为什么是 P0**:AI PM 面试和普通 PM 面试最大的差异——AI PM 必须**显式回答"哪个数能验证我的设计是对的"**。目前 §3.1–§3.4 四条设计决策都没有配套指标,等于面试时讲完故事接不下来。

**交付物**:

新建 `METRICS.md`,**关键不是面面俱到,而是每条设计决策配 1–2 个验证指标**:

| 设计决策 | 验证指标(主) | 健康阈值假设 | 数据来源 |
|---|---|---|---|
| §3.1 双重确认闭环 | 招聘方→应聘方双重确认通过率 | ≥ 75% | T2 + T3 埋点 |
| §3.1 双重确认闭环 | 拉群后 24h 内退群率 | ≤ 5%(对比 v2 应该显著下降) | 群聊事件 |
| §3.2 自动递补 | 平均成单时长(发布 → 已成单) | ≤ 24h | 状态迁移日志 |
| §3.2 自动递补 | 递补激活成功率(备选未耗尽前成单) | ≥ 60% | REPLACING 转移日志 |
| §3.3 统一取消 | 招聘方主动取消频次(P10–P90 分位) | < 10% | CANCELLED 事件 |
| §3.4 三段超时 T1 | T1 超时频次 / 中介总响应数 | ≤ 15% | T1 触发日志 |
| §3.4 三段超时 T2 | T2 超时后异议重筛成功率 | ≥ 50% | DISPUTED 转移日志 |
| §3.4 三段超时 T3 | T3 超时频次 / 应聘方应答总数 | ≤ 20% | T3 触发日志 |

最上层定义 **1 个北极星指标**:**"双确认成单率"**(从发布到已成单且 7 日内未取消的招募令占比)——这个指标同时反映了 §3.1 + §3.2 + §3.4 三条决策的健康状况,且不被刷量游戏(因为要求 7 日不取消)。

附:**反指标 / 护栏指标**:① 应聘方端 NPS 不能跌(防止系统替用户决策伤了体验);② 客服工单率不能涨(防止 §3.3 统一取消滥用);③ 中介方 #17 提交平均耗时不能涨(防止 §3.2 让中介负担过重)。

**投入估计**:4–6 小时。

**面试转化**:能稳住"你的北极星指标是什么 / 你怎么验证这个设计是对的"这两个高频问题。

---

## P1 —— 值得补,做完显著加分

### P1-1 · 竞品分析(2–3 个,聚焦差异点)

**对应短板**:`AI_PM_DIAGNOSIS.md` §1.4 商业 sense 的零提及。

**交付物**:新建 `COMPETITIVE_ANALYSIS.md`,选 **青团社 + 兼职猫**(直接竞品,双方撮合)+ **BOSS 直聘兼职板块**(单边导流模式)。**不要做大而全的功能矩阵对比**(那是浪费时间),只回答三个问题:

1. **撮合模式差异**:他们是"应聘方 ↔ 招聘方"双方撮合,本产品是"应聘方 ↔ 中介 ↔ 招聘方"四方协作——多出来的中介方解决了什么、增加了什么成本?
2. **筛选效率差异**:他们靠应聘方海投 + 招聘方海筛,本产品让中介方做集中筛选——单位招募令的筛选成本谁更低?信任度谁更高?
3. **AI 介入空间差异**:他们已经在 AI 推荐方向投入很久(成熟竞品的护城河),本产品的 AI 介入空间在哪里**反而更大**?(回答:中介方筛选场景天然产出"高质量人工标注样本",这是双方撮合产品拿不到的训练数据资产)

**投入估计**:6–8 小时(关键是把核心差异点想透,不是把竞品截图截完)。

**面试转化**:"你看了哪些竞品 / 你的产品的护城河是什么"——直接答得上。

---

### P1-2 · 补关键路径原型(应聘方端 #19a + 招聘方端 #18)

> ✅ **2026-05-18 已扩展**:在原 #19a/#18 两屏(`dual-confirm-prototype.html`)之外,按用户指令新建 `part_time_job_wechat_mini_program_prototype/full-flow-prototype.html`——**三端关键决策节点端到端一条流**(应聘:#11 推送→#14 报名→#19a 确认→成单/完工;招聘:#9 发布→#18 确认名单→T11 完工确认;中介:报名池→#17 提交 rank)。复用 cream 外壳/VC token byte-identical;#18/#19a 两屏原样复用。AI 仅在 3 个自然落点(应聘 #11 推送、招聘 #9 字段、中介 #17 报名池)显式标记 **「v2 · 本版不做」** + 页眉全局 AI 说明,绝不呈现成 MVP 已实现。

**对应短板**:`AI_PM_DIAGNOSIS.md` §1.3 设计完整度的"只覆盖 1/4 视角"。

**交付物**:

复用 `model_selection_business/cream-dossier.html` 已经搭好的 `IOSDevice` + iOS 外壳骨架(不必新搭手机壳),按相同的 V{A,B,C} 命名风格新增:

1. **应聘方 #19a 确认接受屏**:这是 v3 双重确认闭环的**点睛动作**(诊断报告 §1.5 第一卖点的最后一笔)。需要画清楚:24h 倒计时怎么呈现、"接受 / 放弃"两个按钮的视觉权重、放弃后会发生什么的提示文案、谁会被通知。
2. **招聘方 #18 确认名单屏**:中介方提交筛选结果后,招聘方看到的是什么——rank 列表、AI 推荐理由(承接 P0-1)、"全部接受 / 退回重选 / 单条剔除"的交互。这一屏同时还是 P0-1 AI 模块的**视觉落点**(让 AI 推荐理由可见)。

**注意**(技术约束,来自 `CLAUDE.md`):

- 不要用 JSX,跟现有原型一样用编译后的 `React.createElement`
- 末尾 IIFE 里挂 `InteractivePhone`,通过 `Object.assign(window, ...)` 暴露组件
- 全中文,不要英文 placeholder

**投入估计**:6–10 小时(因为骨架可以复用)。

**面试转化**:被问"你的核心动作的交互长什么样"时能直接打开看。

---

### P1-3 · 埋点设计(配合 P0-2)

**对应短板**:`AI_PM_DIAGNOSIS.md` §1.1 产品深度的"无数据思考"。

**交付物**:在 PRD 里新增 §9 或独立 `TRACKING_SPEC.md`,把 P0-2 `METRICS.md` 里每条指标反推到事件埋点:

- 事件名(规范:`{domain}_{action}`,如 `recruitment_state_changed`)
- 事件 schema(字段、类型、是否必填)
- 触发时机(对齐到 §4 主流程消息编号 + §5 状态迁移)
- 关联指标(回链到 `METRICS.md`)

**投入估计**:4–6 小时。

**面试转化**:体现"产品验证设计假设,要把假设拆到事件粒度"的工程化能力——这是 AI PM 与"只会画图的 PM"的关键区分。

---

## P2 —— 有余力再做

下列三项**不做也能投简历**,做了能让作品集更厚——但应该在 P0+P1 完成之后再考虑。

- **用户访谈记录**(2–3 份):学生兼职用户 + 中介从业者(可以是简化版,1 页/份)
- **商业模式画布**:Take rate / 单价假设 / GTM 渠道 / LTV-CAC 估算
- **v2 详细方案**:分级补偿 / 多维匹配引擎升级路径——可以呼应 P0-1 的"模型演进路径"

---

## 路线图总览

```
✅ 已完成  P0-1 AI 规格(重定位为 v2 愿景:AI_MATCHING_SPEC.md + PRD §6.3/§8)
✅ 已完成  P1-2 #19a/#18 关键路径原型(dual-confirm-prototype.html)
✅ 已完成  P0-2 METRICS.md(指标设计/验证假设体系,北极星=双确认成单率)
✅ 已扩展  P1-2 三端端到端流(part_time_job_wechat_mini_program_prototype/full-flow-prototype.html)
✅ 已完成  门面+诚实止血(README 重写 + 诊断/路线图去过期·修死链,2026-05-18)
          ─── P0 两项 + P1-2 完成:技术/产品面可投;但⚠ ───
⚠ 投前补  P1-1 竞品分析 + INTERVIEW_NOTES.md(未建)——商业 sense 仍 ☆,
          "竞品怎么看 / 北极星怎么讲"是面试翻车点,正式海投前补这两项再投
下一步     P1-1 竞品分析 → INTERVIEW_NOTES.md → P1-3 埋点设计 → P2 任选
```

**重要提醒**:每完成一项,**回头把 talking points 加到一份"面试自答稿"里**(可以是 `INTERVIEW_NOTES.md`)。作品本身打磨得再好,临场讲不出来也是浪费——把"会讲"和"做出来"同等对待。

---

## 更新记录

- **2026-05-20(v2 增量修正)**:**应聘方评价按钮 flex 错位修复 + 招聘方 done 态加 2 张应聘方评价卡(单张独立发布)**——用户实测同日 v1 落地版后给出两个修正:① `ApplicantDeal` 评价表单底部 `display:flex` 横排 `[Primary 发布, Ghost 取消]` 错位(根因:`Primary`/`Ghost` 均 `display:block, width:100%` line 1226-1227,两个 100% 宽按钮塞 flex 容器必然挤错);② 招聘方 `EmployerComplete` (line 1466-1470) `st==='done'` 直接给 `StatusCard` 终态 + Reset,缺评价应聘方的入口。AskUserQuestion 三拍板:招聘方评价**逐个评**(2 张并列卡 · 陈一鸣/林小麦 · 可**单张独立发布**,不必同时填完)/ window 桥**不扩展**(招聘方评价仅在自己页面终态显示,应聘方页不感知 · 保 v1 接桥点零变更)/ 终态视觉**保留 StatusCard + 累加**"评价已发布"footer 小卡(不改写 StatusCard 大卡)。**2 处净改(两文件锁步,grep 引用计数 8 类全对称 6/6·5/5·1/1·1/1·4/4·2/2·2/2·2/2)**:① `ApplicantDeal` line 1395-1397 去 `display:flex/flex:1`,Primary `marginBottom:12` 自然垫高,Ghost 紧随其下各占整行,对齐 `EmployerComplete` 行 1461-1463 既有 Primary/Ghost 排列惯例;② `EmployerComplete` (line 1441-1471) `st==='done'` 分支整段改写:新增 `reviews = { '陈一鸣': {rating,note,published}, '林小麦': {...} }` React state + `updateReview(name, patch)` / `clearReviews()` helper,StatusCard 之下 `ok && [...].map` 渲染 2 张评价卡(完全复用 ApplicantDeal 评价表单 5★ + textarea + Primary 视觉,但 published 后整卡 `opacity:0.55` + VCTab 由 stamp 红改 coffee + 「评价·✓ 已发布」尾巴 + 星级/textarea/按钮全部锁死 `disabled` / `cursor:default` / `onClick:null`),按钮文案 published 后变「✓ 已 发 布」;`allReviewed` (两张均 published) 时追加 footer 小卡 `border:0.5px solid t.coffee` 居中「评价已发布 / 对 2 名应聘方的评价已沉淀为画像数据,本笔招募令招聘方侧旅程闭环」;Reset `onReset` 联动 `clearReviews()` 把 reviews 重置回初始(配合 `setSt('pending') + setLeft(601239) + window.setDealState({employerConfirmedDone:false})` 整页回归初始态)。**应聘方姓名 hardcode 对齐**:`陈一鸣` + `林小麦` 严格沿用 `PlatformGroupAttend` APP 数组 + `ApplicantMine` 主人公字段(memory `project_intermediary_is_platform_side` 一致),不引入新人名。**编辑禁区 / 铁律自检 PASS**:无 JSX(纯 `React.createElement`)、`window` 桥零变更(v1 接桥点 5 处全保 + 不扩展 `dealState`)、全中文、`casting-prototype-standalone.html` / `__om-edit-overrides` / **PRD §5 状态机 / #1-#22 / T1-T11 / SVG / diagram-1-flow.svg 零变更**(招聘方→应聘方评价沿用 PRD §4 表 #23-#24 互评语义,不引入新枚举)、零 AI 文案(评价全是人工动作)、cream-dossier 令牌全复用(paper/coffee/stamp/D/S/M + Avatar + VCTab + Primary,零新组件零新色卡)、两文件 byte-identical 锁步(`project_full_flow_two_files_lockstep` 严格 PASS)。**ROI 定位**:产品深度(§1.1)+ 设计完整度(§1.3)+ 可讲性(§1.5)增量补强;**对 AI 含量(§1.2)零增益**。叙事卖点 = "v1 实现了应聘方→招聘方单向评价,v2 补完成**完整双向评价闭环原型**;**单张独立发布**是一个 v1 没想清楚的产品判断 — 两位应聘方独立履约,评价也应独立提交,这就是为什么不做'整体一次评'的退化路径(逐个评 = 真实业务的细粒度反馈)"。**面试 talking point**:可讲 1-2 分钟"我用 React 对象 state(`{陈一鸣: {published}, 林小麦: {published}}`)的不可变更新 pattern 实现单张独立发布,而不是用扁平 `[published1, published2]` 数组 — 用 name 作 key 能在评价人数 v2 扩展到 N 张时零结构变更,只换数据源;另外 v2 选择**不扩展 window 桥**让应聘方页感知双方评价完成,是因为真实业务里双方评价**应该匿名互不可见**,过早把'双方评价已完成'文案 fan-out 到对方页是把后端业务规则泄漏到原型,所以保 v1 单向 flag 模型"。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。*(v3 微调 同日)* footer 小卡按用户实测视觉精修:去 `border: 0.5px solid t.coffee` 外框、`padding: 14px 14px` 去除、标题由「已评价 · 双向评价已发布」简化为「评价已发布」(信息密度更聚焦,与上方两张 paper 评价卡的纸感不再竞争层级)、斜体说明原文保留;两文件继续 byte-identical 锁步(grep `双向评价已发布` 两文件均归 0,`评价已发布` 两文件各 2 处对称 — 标题 1 + 应聘方页闭环文案 1)。
- **2026-05-20**:**应聘方「我的成单 / 完工进度」状态机重构(从「自己点推进」改为「四外部信号派生」· 评价表单收尾)**——用户实测后指出 `ApplicantDeal` 用 `setStep(step+1)` 让应聘方自己点 5 次走完全流程,**反语义**(真实业务里应聘方不能自己决定自己处于哪个状态)。重构为:状态由 4 个外部信号派生 ① 中介方点「✓ 全员入群」② 应聘方在打卡屏点「我已完工」③ 招聘方点「确认完工」④ 应聘方填评价表单点「发布」。AskUserQuestion 两拍板:step 2→3 触发=招聘方点「确认完工」+ 应聘方加「我已完工·待验收」中间态(选项 C,与 PRD §5 WORKING 兼容仅作展示层细化);评价交互=完整表单(星级+评语+发布)。**5 处净改(两文件锁步,grep 引用数对称 4/4/5/5/4/4/6/6/1/1)**:① 新增 `window.dealState` / `setDealState` / `resetDealState` 全局桥(`step` 由 4 个布尔 flag 派生,避免不一致),挂在 `Object.assign(window,...)` 之前;② 重写 `ApplicantDeal`:`React.useState(0)` 本地态删除→改 `useReducer` 强刷 + `useEffect` 注册 `dealStateListeners`,`line` 数组 5→6 项(新增 step 3「我已完工·待招聘方验收」、step 4 改「工作完成」、step 5 改「待评价」),删「推进一步 →」按钮,step 0–4 改渲染 muted italic 等待提示("等待中介方点…"/"等待招聘方在 T11 7 天内验收"等),step 5 加「进行评价 →」Primary 弹评价表单(5 ★ 文字按钮 + textarea 评语 + 「发布评价 →」, rating > 0 才可发布),已发布显示「评价已发布·一笔招募令的应聘方旅程到此闭环」终态,Reset 调 `window.resetDealState()`;③ `PlatformGroupAttend`「✓ 全员入群」`onClick=null` 接 `setDealState({groupAllJoined:true})` + 触发后置灰为「✓ 已触发履约·应聘方页同步」;④ `ApplicantCheckIn` 当 `status==='arrived'` 时同步 `setDealState({applicantDeclaredDone:true})`;⑤ `EmployerComplete`「确认完工」接 `setDealState({employerConfirmedDone:true})`,Reset 联动清除该 flag(「有异议」路径不接桥,异议走 PRD §6.2 不进评价流)。**编辑禁区 / 铁律自检 PASS**:无 JSX(纯 `React.createElement`)、`window` 桥新增 3 个键完整、全中文、`casting-prototype-standalone.html` / `__om-edit-overrides` / **PRD §5 状态机 / #1-#22 / T1-T11 / SVG / diagram-1-flow.svg 零变更**(step 3「我已完工·待验收」定位为**原型展示层对 PRD WORKING 的细化**,不引入新 PRD 状态枚举,后端不动)、零 AI 文案(状态推进全是人工动作)、cream-dossier 令牌延续(paper/coffee/stamp/D/S/M)、两文件 byte-identical 锁步(`feedback_match_cream_design` + `project_full_flow_two_files_lockstep`)。**ROI 定位**:产品深度(§1.1)+ 设计完整度(§1.3)+ 可讲性(§1.5)三维补强;**对 AI 含量(§1.2)零增益**——叙事卖点 = 把「单角色单文件 mock」升级为「含跨角色状态机的可观测原型」,Title sub 文案直接亮明设计意图("此页状态不由你决定·由中介/你/招聘方/你四个外部信号派生")。**面试 talking point**:这段叙事可讲 2-3 分钟"我识别到原型有反语义 bug——应聘方不能自己决定自己的成单状态——重构成四方动作触发的派生状态。产品判断三层:① 状态主体识别(谁有权推进谁就触发)② 用 4 个布尔 flag 派生 step 而不是直接存 step(避免 flag 与 step 不一致 + 终态语义清晰)③ 评价发布作为最后一步触发,把「确认完工→闭环」改为「确认完工→待评价→闭环」,更贴近真实业务的双向评价机制"。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20**:**中介方「待入群」待办卡 + `PlatformGroupAttend` 详情屏(产品深度补强 · 中介工具闭环延展 · 非 AI 含量补强)**——用户指定:中介方待办页加一张「待入群」卡,详情页能分别催促招聘方与应聘方入群,可标记某方为「已入群」,应聘方与招聘方视觉区分,**风格类似入选名单**。AskUserQuestion 两拍板:卡位 = 已成单·待建群之后、alt 3 之前(语义递进:#20-21 建群 → #22 待入群 → alt 3 异议);应聘方 = 多张候选人卡并列(对齐 ZARA RLIST[0].need=2 → 陈一鸣 + 林小麦 2 张)。**5 处净改(两文件 byte-identical)**:① `PlatformTodo` L 数组在「已成单·待建群」(原第 4 卡)与「alt 3 异议重筛」(原第 5 卡)之间插入新卡 `{stage:'#22 · 待入群', u:true, title:'ZARA 西单 · 待双方扫码', left:'剩 12h', due:'今日 24:00', goto:'groupattend', recruitId:'r1'}`,ListHint 计数 5→6(含 1 项可立即处理 → 2 项预警);② 新增 `function PlatformGroupAttend(props)`(位于 PlatformWaitDetail 与 PlatformMine 之间)— 内部 `memberCard` helper 参照 EmployerRoster Card 视觉(VCTab + Avatar + 名/tag + 状态区 + 催办 Ghost + 标记 Primary),用 React.useState 管理 `{ employer: {joined, urged}, applicants: [{joined,urged}, {joined,urged}] }` 对象状态;招聘方 1 张(ZARA 西单大悦城店)+ 应聘方 2 张并列(陈一鸣 + 林小麦,与 ApplicantMine 主人公字段对齐 + 新增林小麦呼应 #18 入选 2 人语义);已入群时整卡 `opacity: 0.55` + VCTab 由 stamp 红改 coffee + 「已入群 ✓」尾巴 + 催办按钮消失;催办按钮 toggle 复刻 PlatformWaitDetail dispute 模式「催办对方 → ↔ ✓ 已催办 24h 内不可重复」状态机;全员入群时底部条件渲染 Primary「✓ 全员入群 · 进入履约」(静态文案,遵 cream 静态原型纪律);③ `Object.assign(window, ...)` 加 `PlatformGroupAttend: PlatformGroupAttend`(同 PlatformGroupSetup 同列同段);④ `InteractivePhone` 加 `'groupattend'` view 分支 `e(window.PlatformGroupAttend, { recruitId: top.recruitId })`;⑤ `backTo` 对象加 `groupattend: 'todo'`(`hideTab` 经 `noTab = backTo.hasOwnProperty(v)` 自动包含,无需新增 hideTab 列表项)。**两文件锁步 grep 自检 PASS**:`PlatformGroupAttend` / `groupattend` / `待入群` 累计 9 处两文件完全对称;PlatformWaitDetail / PlatformMine 函数行号锚点未漂移。**编辑禁区 / 铁律自检 PASS**:无 JSX(纯 `React.createElement`)、`window` 桥完整新增 1 项、全中文(零英文 placeholder)、`casting-prototype-standalone.html` / `__om-edit-overrides` 未碰、**PRD 编号 #1–#22 / T1–T11 / SVG / 状态机 / alt 3 强约束链路零变更**(原 alt 3 卡仅从第 5 卡变第 6 卡,goto/kind/recruitId 字段不动)、零 AI 文案(memory `feedback_ai_is_future_vision` — 催办与标记入群均为人工动作,不复用 AiBanner / 不标 v2)、cream-dossier 令牌延续(memory `feedback_match_cream_design` — 一律 paper/coffee/stamp/衬线,无新色卡);ApplicantDeal / EmployerMine 等其他屏未触及;memory `project_intermediary_is_platform_side` 一致(此屏中介独享,不在应聘/招聘 tab 出现);memory `feedback_chrome_polish_is_intentional` 一致(IOSDevice / WeChatCapsule / tabBar 等外壳零改动)。**ROI 定位**:产品深度(§1.1)+ 中介方交互闭环(§1.3)双轴补强 — 把中介工具从「上传 QR」一刀切扩为「上传 QR → 跟进入群 → 全员入群完成」可观测三段;**对 AI 含量(§1.2)零增益**。**面试 talking point**:这段叙事可讲 2 分钟"PRD §6.2 已留位'运营推送/催办',原型把它落地为'中介视角看到双方各自入群状态、可分别催办'—— 关键的产品判断有三:① 中介视角而非某一方视角(memory `project_intermediary_is_platform_side`),② 招聘方 1 张卡 vs 应聘方多张卡的不对称展示(反映用工关系本身一对多),③ 催办按 24h 频控 = §3.7 软推进对称叙事 + §3.4 不替决策叙事的产物"。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20(v3 修订)**:**入群通告绑 ZARA 西单(RLIST[0]主线)+ 详情页加 Ghost 跳招募令详情**——用户实测 v2 后反馈:① "我的"页入群通知 EntryRow **没标明是哪个招募令**;② 点进详情后 **没有跳转到招募令详情的入口**。AskUserQuestion 三拍板:绑 RLIST[0] ZARA 西单(与 #9/#18/#19a/T11/ApplicantJobDetail hardcode 全部对齐 — 顺便修正 v2 残留的"问卷地推 · 三里屯"占位失谨)/ Ghost 按钮放底部 CTA 区(Primary 之上)/ EntryRow sub 文案 「ZARA 西单 · #22 已上传二维码」(招募令名前置)。**3 处净改(两文件 byte-identical)**:① `ApplicantMine` + `EmployerMine` sub 文案统一改 ZARA;② `AnnouncementDetail` Title sub + paper 卡描述行从"问卷地推 · 三里屯"重写为"ZARA 西单大悦城店 · 周末品牌快闪",底部 Primary 之上加 `e(Ghost, { onClick: props.onJump }, '查 看 招 募 令   →')`;③ `InteractivePhone` `'announcement'` 分支加 `var jumpView = props.role === 'employer' ? 'recruitdetail' : 'jobdetail'; ... onJump: function () { push({ view: jumpView }) }` —— 应聘方 role 跳 ApplicantJobDetail(屏 1.5 hardcode ZARA 兼职导购,与本次绑定零额外配)、招聘方 role 跳 EmployerRecruitDetail(组件内 `var base = (props && props.rec && props.rec.cat) ? props.rec : RLIST[0];` 行 1776 已有 fallback,**不需传 rec、不需新增 window 桥**)、平台/中介不消费通告不会触发无分支需写。**编辑禁区 / 铁律自检 PASS**:无 JSX、`window` 桥未变(`AnnouncementDetail`/`EmployerRecruitDetail`/`ApplicantJobDetail` 已就位)、全中文、`casting-prototype-standalone.html` / `__om-edit-overrides` / PRD 编号 #1–#22 / T1–T11 / SVG / 状态机零变更、零 AI 文案、cream-dossier 令牌延续(Ghost 是 cream helper);`hideTab` v1 已含 `'announcement'`/`'jobdetail'`/`'recruitdetail'`,跳转后子页隐藏 tabBar、返回靠 stack pop 自动回上层(announcement → jobdetail 后再 pop 回 announcement,再 pop 回来源 tab/Mine 页);两文件锁步 grep 自检:`ZARA 西单 · #22` 各 2 处对称、`onJump` 各 1 处对称。**ROI 定位**:产品基本功微调 —— 把"通用入群提示"升级为"绑某笔招募令的入群提示 + 可一键跳详情",信息架构闭环度提升(原来从入群通知去找招募令需要回退跨页);**对 AI 含量(§1.2)零增益**;**性质 = v2 修订的延展精修**。**面试 talking point**:这段叙事可讲 1 分钟"通告与业务对象绑定的产品判断 —— 入群通告必然挂在某笔招募令上,详情页提供一键回流到招募令详情的入口,避免用户跨页找上下文;同时按 role 分流到不同详情形态(应聘看岗位详情、招聘看自己招募令的修改入口)是同一 view 抽象在两端的差异化落地"。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20(v2 修订)**:**通告条样式重做 + 我的页换 EntryRow accent + 详情页改入群通知**——用户实测同日 v1 落地版后给出 4 条修正:① 通告条**全宽、`#1989fa` 微信蓝、白字、喇叭 20px(用户拍板,比字号 14px 大更醒目)、最右 × `useState` 关闭**(原 paper 卡浅咖啡 + 22px 横向 margin 推翻);② 文案改入群通知「中介林之航 已上传微信群二维码,请尽快扫码入群」(原"7/1 结算窗口缩短"运营公告留作 v2 talking point);③ `ApplicantMine` 删 `AnnouncementMineCard` 卡片 → 新增 `EntryRow accent`「入群通知 · 扫码进群 / #22 · 中介已上传群二维码」(图 1 红边框红字款,与「录用通知」「成单进度」三个 accent 入口并列);④ `EmployerMine` 删除原 QR 大卡(行 1867-1873)+ 删 `AnnouncementMineCard` → 同样 `EntryRow accent`「入群通知 · 扫码进群」(三 accent 入口);⑤ `AnnouncementDetail` 重写为入群通知详情(承接原 QR 大卡内容:VCTab「群通告 · 图片」+ ▣ QR 占位 180px + 长按保存文案 + #22 fan-out 注脚)。**6 处 Edit / 两文件双应用(byte-identical)**:`AnnouncementBar` 函数体 / `AnnouncementMineCard` 函数体(整段删) / `ApplicantMine` 卡 → EntryRow 替换 / `EmployerMine` QR 卡 + Card 双删 + EntryRow 新增 / `AnnouncementDetail` 函数体 / 路由 `announcement` 分支注释更新。**编辑禁区 / 铁律自检 PASS**:无 JSX(纯 `React.createElement`)、`window` 桥未变(`AnnouncementDetail` 导出保留,`AnnouncementMineCard` 本就未导出无桥需清)、全中文、`casting-prototype-standalone.html` / `__om-edit-overrides` / PRD 编号 #1–#22 / T1–T11 / SVG / 状态机零变更、零 AI 文案(memory `feedback_ai_is_future_vision`)、cream-dossier 令牌仍主导我的页/详情页(memory `feedback_match_cream_design` — 仅顶部 chrome 条采用微信蓝是合理的"系统级通告"语义,与卷宗主体内容分层)、两文件 `AnnouncementMineCard` / `群通告 · 图片` 残留 grep 双验证(`AnnouncementMineCard` 零残留 / 「群通告·图片」仅在 ApplicantDeal #1348 + AnnouncementDetail #2016 合法保留)。**ROI 定位**:产品基本功补丁的样式向量修正——更贴近真实微信小程序的"系统级通告条"视觉语言(蓝底 + × 关闭);我的页入口统一为 accent EntryRow 后**信息架构更对称**(原 paper 卡 vs accent EntryRow 双形态混杂被收敛为单一形态);**对 AI 含量(§1.2)零增益**。面试 talking point 微调:从"我加了一层运营广播抽象"改为更诚实的"原型阶段我先用最小素材(入群通知)把通告通道走通,通用运营广播 = v2"——更稳。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20**:**应聘/招聘双端「小程序通告」运营广播条 + 我的页入口 + 详情页(产品基本功补丁 · 非 AI 含量补强)**——用户指定:紧贴微信胶囊栏下沿加一条小喇叭 + 通告事件文本,点击进详情页;「我的」页也要有入口,样式仿 `EmployerMine` 行 1867 已有的「群通告 · 图片」卡(用户原话"和入群通知相似")。三个产品判断(AskUserQuestion 拍板):① 通告条覆盖范围 = 应聘方 browse/schedule + 招聘方 erecruit 共 3 个 tab,我的页(cmine/emine)由卡片入口承担,不重复贴(平台 list/todo/pmine 不显示,因中介方=发送侧);② 通告内容定位 = 中介林之航发的"7/1 起结算窗口由 7 天缩短至 5 天",挂在 PRD §6.2「平台运营推送」自然位、与现有「群通告 · 图片」共享同一发送方人设;③ 我的页入口位置 = 两条 accent EntryRow 之后、`ListHint('历史订单')`/`ListHint('招募令订单')` 之前(优先级语义:紧急事件 > 运营广播 > 历史)。**3 处净增(双原型 byte-identical)**:① `EntryRow`(行 1516)之后新增 `AnnouncementBar` + `AnnouncementMineCard` 两组件(前者单行可截断喇叭条带 9px 浅 coffee 边、后者复刻群通告卡骨架去 QR 占位);② `Object.assign(window,...)` 之前新增 `AnnouncementDetail` 详情页(Title「通告 · NOTICE / 中介方 / 广播」+ paper 卡含正文 + 「我 已 知 悉」Primary `onAck={pop}` 触发 stack pop);③ 路由 `InteractivePhone` 三个 tab 分支 ApplicantBrowse / ApplicantSchedule / EmployerRecruitList 各加 `onAnnouncement: ()=>push({view:'announcement'})`;新增 `'announcement'` view 分支 `e(window.AnnouncementDetail,{onAck:pop})`;`hideTab` 列表追加 `v==='announcement'`(详情页隐藏 tabBar);`Object.assign(window,...)` 加 `AnnouncementDetail`。**编辑禁区 / 铁律自检 PASS**:无 JSX(纯 `React.createElement`)、`window` 桥完整新增 1 项、全中文、`casting-prototype-standalone.html` / `__om-edit-overrides` / **PRD 编号 #1–#22 / T1–T11 / SVG / 状态机 / 招聘方招募令修改链路全部未碰**;零 AI 文案(memory `feedback_ai_is_future_vision` 纪律延续 —— 通告是纯人工运营动作,不复用 AiBanner、不加 v2 标记);延续 cream-dossier 视觉令牌(memory `feedback_match_cream_design` —— 仅用 paper/coffee/S/M/D token,零新色卡);两文件 Announcement/announcement grep 出现次数 17/7 完全对称(锁步 `project_full_flow_two_files_lockstep` PASS)。**ROI 透明声明**:产品深度(§1.1)+ 设计完整度(§1.3)轻度补强(补齐 PRD §6.2 已预留但未在原型表达的"平台运营推送"触达通道,跨业务流广播层与 #1–#22 业务流消息层 / T1–T11 时序提醒层并列形成第三层运营触达栈);**对 AI 含量(§1.2)零增益**;**性质 = 产品基本功补丁,非新增产品决策**(故 PRD §6 异常表 / §5 状态机不动)。**面试 talking point**:这段叙事可讲 1 分钟"我识别到原型缺一层跨业务流的运营广播层 —— PRD §6.2 写了'平台运营推送'但原型一直没表达 —— 补上后形成业务流编号 / 时序提醒 / 运营广播 三层触达栈,而中介方作为发送侧不消费自己发出的广播(发送侧工具 = v2 范围 talking point)"。**⚠ ROADMAP 优先级提醒**:本次属用户拍板的产品基本功补丁,**P1-1 竞品分析仍是空缺**——投简历前必补,通告功能不能替代它。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20**:**alt 3 路径下 AgentPool 强约束(隐 #17 提交 + 标记被异议候选)**——承接同日 bug-fix 细化。用户在确认 Ghost 按钮可见范围(保持现状·所有 pool 入口都显示)后追加要求:**alt 3 异议重筛路径下,报名池不可出现 #17 提交按钮 + 标红被招聘方异议的候选 + 只留 Ghost 重新筛选**。产品判断 = 招聘方异议触发的中介**不能"原样重交"**(等于没修订),必须经过 ① 看见被异议候选 → ② 走 Ghost 回名录 → ③ 重新选/排序 → ④ 第二次到 pool 时(新 push 不带 mode)才允许提交——这套强约束闭环用 **React state push 链自然实现 first/dispute mode 切换**,无需新增状态字段或 Redux。**3 处改动(双原型 byte-identical)**:① AgentPool 函数体加 `var mode = (props && props.mode) || 'first'`,Title sub / rows.map 内 paper border / 右上角 VCTab 红章「招聘方异议」(对齐 PlatformWaitDetail dispute paper 卡占位的"候选 #2 rank 1 + 候选 #5 rank 3" → `i===0 || i===2`)/ Primary `mode !== 'dispute' &&` 条件渲染 / 底部说明文案 5 处随 mode 切换;Ghost 不动;② 路由 `'waitdetail'` onSubmit push 时根据 `top.kind === 'dispute'` 自动带 `mode: 'dispute'`(路由层判断,不需改 PlatformWaitDetail 组件);③ 路由 `'pool'` 分支 `e(window.AgentPool, { mode: top.mode, onRestart: ... })` 透传 mode(undefined 时 AgentPool 默认 first)。**双文件 byte-identical 自检 PASS**:AgentPool(行 1434-1478)+ waitdetail/pool 路由块(行 3249-3257)2 区段 diff 通过。**编辑禁区铁律 PASS**:无 JSX、`window` 桥未变(无新组件)、全中文、`casting-prototype-standalone.html` / `__om-edit-overrides` 未碰、**PRD / SVG / 状态机 / PlatformTodo / PlatformWaitDetail / VCApplicants 全部未碰**、零 AI 文案(AgentPool 现有 v2 AiBanner 未动)、不破坏 first mode(`first` 默认值 + 所有条件渲染都有原分支回退)、不破坏招聘方旅程(招聘方不走 pool)。**ROI 定位**:产品深度(§1.1)+ 设计完整度(§1.3)+ 可讲性(§1.5)三维补强;**对 AI 含量(§1.2)零增益**;**性质 bug-fix 细化非新增产品决策**(故 PRD §6.1 alt 3 时序图保留不动,本次约束仅在原型层面表达)。**面试 talking point**:这段叙事可讲 1-2 分钟"我用 React push 链的状态语义(新 push 自动覆盖 stale prop)实现了 alt 3 异议重筛的产品强约束——dispute mode 进 pool 时隐 Primary 强制走 Ghost,Ghost→名录→再 push 回 pool 时自动恢复 first mode 可点提交,**整套闭环零额外状态字段**"。**未改 P0/P1 排序,下一步仍是 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20**:**中介方报名旅程入口 bug-fix(T1 + alt 3 走「报名名录 → 报名池」)**——用户指出上轮 PlatformTodo 5 卡全可点击后,T1 预警·待筛和 alt 3 异议重筛**都直接跳了 `'pool'` (AgentPool 报名池)**,跳过了"先看报名名录(VCApplicants)再排序进池"的产品步骤,与招聘方旅程 list→applicants→pool 的设计不一致。本次定位**bug-fix 性质修复**(非新增产品决策,PRD 不动)——补齐"全集浏览 vs 决策排序"两阶段的中介视角入口。**5 处改动(双原型 byte-identical)**:① PlatformTodo L 数组 T1 卡 `goto:'pool'`→`goto:'applicants'`,T1/T2/T3/alt 3 各加 `recruitId:'r1'/'r2'/'r3'/'r4'`(对应 RECRUITS 现成 id);② onClick 签名 `props.onOpen(d.goto, d.kind, d.title, d.recruitId)` 增加第 4 参;③ AgentPool 函数签名 `()`→`(props)`,Primary "提 交 #17" 之下加 Ghost `'重新筛选 · 回报名名录 →'` 触发 `props.onRestart`,说明文案补一句承接 §6.1 alt 3 重筛语义;④ PlatformWaitDetail dispute 模式 Primary `onSubmit('pool')` → `onSubmit('pool', props.recruitId)`,让 alt 3 跳 pool 时携 recruitId;⑤ 路由 InteractivePhone 三处:`'todo'` onOpen 签名扩透传 recruitId、`'waitdetail'` 接 `top.recruitId` 并 onSubmit 透传、`'pool'` 加 `onRestart: function () { push({view:'applicants', recruitId: top.recruitId || 'r1'}); }` 替换原 `null`(兜底默认 r1 避 VCApplicants 崩)。**新的中介旅程**:T1 首筛 = `todo → applicants → pool → 提交 #17`(标准路径);alt 3 异议重筛 = `todo → waitdetail(dispute) → pool(看上次 rank) → [底部 Ghost] → applicants → pool → 提交`(按用户原话"在报名池页面底部有重新筛选按钮进入报名名录页面")。**双文件 byte-identical 自检全 PASS**:PlatformTodo / AgentPool / PlatformWaitDetail / 路由块 4 区段 diff 通过;函数行号锚点对齐。**编辑禁区 / 铁律自检 PASS**:无 JSX 泄漏、`window` 桥未变(无新组件、不需 Object.assign 改动)、全中文、`casting-prototype-standalone.html` / `__om-edit-overrides` 未碰、**PRD 编号 / 状态机 / SVG / diagram-1-flow.svg 零变更**、零 AI 文案(AgentPool 现有 AiBanner 是 v2 标记未动)、不触发 `feedback_pair_counterpart_screens`、不破坏既有招聘方 list→applicants→pool 链路('applicants' 路由分支未碰、VCApplicants 自带 onSubmit→pool 路径保留)。**ROI 定位**:产品深度(§1.1)+ 设计完整度(§1.3)轻度补强;**对 AI 含量(§1.2)零增益**;**性质 bug-fix 非新增决策**(故 PRD 不动,与本日 §3.6/§3.7 等"新增决策"轮次拉开距离)。**未改 P0/P1 排序,下一步仍是 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20**:**中介方待办卡全可点击 + `PlatformWaitDetail` 等待详情屏 + 催办机制 §3.7(产品深度补强 · 中介方交互闭环 · 非 AI 含量补强)**——用户指定:上一轮 PlatformTodo 改成 5 项但**仅"已成单·待建群"卡可点**,本次把**全部 5 卡做成可钻取入口**,并新增"等待详情屏"承接 T2/T3/alt 3,**详情屏底部加催办按钮**。三个产品判断(AskUserQuestion):① T1 跳 `AgentPool` (报名池,复用现有屏);② T2/T3 跳 `PlatformWaitDetail` (kind='employer'/'applicant') + 底部 Primary "催办对方 →";③ **alt 3 跳 `PlatformWaitDetail`** (kind='dispute',先看招聘方退回原因) + Primary "重新提交筛选 →" 跳 AgentPool;催办按 cream 纪律**纯静态文案**(点击→Ghost 灰态"✓ 已催办·24h 内不可重复",不跑真倒计时);PRD 联动**做最完整版**(§3.4 末加 1 句承接 + 新增 §3.7 完整 callout-decision)。**PRD 两处改动**:① §3.4 callout 末追加"另:T2/T3 倒计时窗口内中介可发软提示(催办),不替对方决策,频控 24h,详见 §3.7"(承接 §3.4 强决策叙事);② **新增 §3.7**(锚点 `#sec-3-7`,§3.6 后 §4 前,无 v2 标记):决策=中介在 T2/T3 窗口可点击催办平台发**非状态变更**提醒至被等方,**不挂主流程编号**(避免 #X 膨胀污染状态变更骨架),24h 频控;取舍=① 不与 §3.4 不替招聘方决策冲突(软提示 ≠ 系统替决策)② 不进 §6.2 异常表(主动加速通道 ≠ 兜底通道)③ 不挂主流程编号(本质是运营推送)④ 24h 频控防滥催伤信任;**最大产出 = §3.7 把 §3.4 的"强决策不对称"叙事补一段"软推进对称"叙事,合起来才完整**——面试 talking point 可讲 2-3 分钟"是否替对方决策 vs 是否允许中介加速是两个独立产品判断"。**原型四处改动(双文件 byte-identical 严格锁步)**:① `PlatformTodo` 5 卡全加 `goto` 字段(T1→pool / T2→waitdetail+kind:employer / T3→waitdetail+kind:applicant / 已成单→groupsetup / alt 3→waitdetail+kind:dispute),`onClick` 简化为无条件、`cursor:'pointer'` 统一,ListHint 改"5 项待办·全部可钻取";② **新增 `PlatformWaitDetail`** (PlatformGroupSetup 后,行 1907-1960,55 行 React.createElement)三态 kind 配置表 + 复用 ApplicantDeal timeline 5 节点模式(#17→#18→#19a→已成单→完工),当前 step 高亮(employer=1 / applicant=2 / dispute=1+红色"已退回")+ 招募令卡(title+当前等谁+静态倒计时+提示文案)+ dispute 模式加"退回原因"paper 卡(2 条静态占位异议)+ CTA 区(employer/applicant: Primary `'催办对方 →'` ↔ Ghost `'✓ 已催办·24h 内不可重复'` 二态切;dispute: Primary `'重新提交筛选 → 报名池'` 调 `props.onSubmit('pool')`)+ 底部小字承接 §3.7/§3.4 引用;③ 路由 `InteractivePhone` 三处登记:`todo` 分支 onOpen 改为透传 `(view, kind, title)`、新增 `waitdetail` 分支传 `kind/title/onSubmit`、`backTo` 加 `waitdetail: 'todo'`(hideTab 经 backTo 自动包含);④ `Object.assign(window,...)` 加 `PlatformWaitDetail`。**双文件 byte-identical 自检全 PASS**:PlatformTodo / PlatformWaitDetail / 路由块 / window 桥 4 区段 diff 通过;函数行号锚点两文件对齐(PlatformWaitDetail 1907-1960、PlatformMine 1961)。**编辑禁区 / 铁律自检 PASS**:无 JSX 泄漏(纯 `React.createElement`)、`window` 桥完整、全中文(`WAIT` 类小标题对齐 cream 现有 `DEAL`/`TODO`/`GROUP` 风格)、`casting-prototype-standalone.html` 未碰、`__om-edit-overrides` 未碰、**PRD 编号 #1-#22 / T1-T5/T11 / alt 1-4 / 现有 sec-X 锚点全保留**(§3.7 仅新增、无重新编号)、§3.4 不替决策叙事完整保留、§5 状态机零改动、§6.2 异常表未碰、**diagram-1-flow.svg 本轮未改**、零 AI 文案(memory `feedback_ai_is_future_vision` 纪律延续)、不触发 `feedback_pair_counterpart_screens`(PlatformWaitDetail 是中介视角独有监控屏无对手屏)。**ROI 定位**:产品深度(§1.1)+ 设计完整度(§1.3)+ 可讲性(§1.5)三维补强;**对 AI 含量(§1.2)零增益**——叙事卖点 = "§3.4 强决策不对称(故意不替招聘方默认通过)与 §3.7 软推进对称(中介可发软提示加速)是 v3 双重确认闭环的两条互补叙事,合在一起回答'AI PM 怎么平衡系统自动化与对人决策的尊重'"。中介方原型从"列表+建群"扩为"列表+5 卡钻取+ 3 路详情(报名池/等待中/建群)+催办"完整闭环。**未改 P0/P1 排序,下一步仍是 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20**:**中介方待办过滤 + #20-#22 建群机制具体化(产品深度补强 / 非 AI 含量补强)**——用户指定:中介方待办只显示两类招募令(① 距招聘令报名截止剩 ≤48h 还没提交 #17 的预警·待筛;② #17 之后到履约前的所有招募令,含 #18 T2 / #19a T3 / 已成单·待建群 / alt 3 异议重筛),双方都 #19a 确认后提醒中介建群,中介上传微信群二维码,小程序以**图片通告**形式 fan-out 至应聘方+招聘方。三个产品判断澄清(AskUserQuestion):预警阈值=招聘令报名截止-48h(为后续 #18 T2 48h + #19a T3 24h 留出安全余量);中介待办全包 5 类(T1 预警 + #18 + #19a + 已成单·待建群 + alt 3);建群+二维码+图片通告**MVP(v3)实现**(非 v2),原因是不涉及 AI、只是平台消息能力扩展。**PRD 五处改动**:① §3.6 新增「建群机制」决策 callout-decision 三段式(锚点 `#sec-3-6`,无 v2 标记,叙事=从"平台抽象拉群"具体化为"#20 通知中介 / #21 上传二维码 / #22 fan-out 图片通告"三个可观测离散事件,根因是微信小程序 OpenSDK 无法代用户建群 + 中介本就是平台预入驻执行方 + 离散事件可埋点);② §4 关键消息表 #20-#22 一行拆 3 行 + 主时序图三条 line/text 重写(#20 方向反转 小程序→中介、#21 改 中介→小程序、#22 标 fan-out 至双方)+ 中介激活条 y=738→686 覆盖 #20-#21 时段;③ `diagram-1-flow.svg` 行 71+136-144 与 PRD 内嵌 SVG **字符级镜像同步**(CLAUDE.md 双 SVG 铁律,grep 自检通过);④ §6.2 异常表"拉群时"→"扫码进群时"(MVP 边缘场景保留);⑤ 附录 A #20-#22 行描述扩展为"中介上传二维码 → 平台 fan-out"(不动 §5 状态机,ROI 不划算);⑥ 附录 B 6 项→**7 项**,新增第 7 行"建群机制"v2 vs v3。**原型四处改动(双文件 byte-identical 严格锁步)**:① `PlatformTodo` 4 项→**5 项**(T1 预警·待筛 / T2 #18 / T3 #19a / 已成单·待建群 / alt 3 异议重筛),删除"待完工确认"(T11 是招聘方动作),函数签名加 `props`,卡片有 `goto` 字段时可点击 push 子页;② 新增 `PlatformGroupSetup` 屏(三态机:empty→uploaded→sent,复用 page/paper/Title/Primary/Ghost helper,零 AI 文案,二维码用 ▣ 占位 + 灰底方块,Primary "发送给应聘方和招聘方·#20-22 →" 切 sent 显示蓝底 fan-out 确认);③ 路由 `groupsetup` 在 `InteractivePhone` 三处登记(view 分支 / `backTo: 'todo'` / `hideTab` 通过 backTo 自动包含),`Object.assign(window, ...)` 加 `PlatformGroupSetup`;④ 双方端图片通告卡——应聘方 `ApplicantDeal` step≥1 时顶部插 paper 卡(▣ 占位 + "长按保存扫码进群"文案)+ line[1]文案从"平台建沟通群"改"群通告·二维码已收到",招聘方 `EmployerMine` Stats 后插同款 paper 卡(静态)。**双文件 byte-identical 自检全 PASS**:`diff sed -n` 校验 PlatformTodo / PlatformGroupSetup / 路由块 / ApplicantDeal / EmployerMine 5 区段;函数行号锚点列表两文件完全对齐。**编辑禁区 / 铁律自检 PASS**:无 JSX 泄漏(纯 `React.createElement`)、`window` 桥完整、全中文(零英文 placeholder)、`casting-prototype-standalone.html` 未碰、`__om-edit-overrides` 未碰、PRD 编号 #1-#22 #19a 全保留(只在 #20-#22 描述层重写,编号未动)、§3.5 v2 标记位未碰、§5 状态机零改动、零 AI 文案(memory `feedback_ai_is_future_vision` 纪律延续)。**ROI 定位**:产品深度(§1.1)+ 设计完整度(§1.3)+ 可讲性(§1.5)三维补强;**对 AI 含量(§1.2)零增益**——讲故事时定位为"识别中介在双方确认后实际手工建微信群+发二维码的真实流程,把它从 v2 平台抽象拉群升级为 v3 可观测离散事件 + 把 PRD §20-#22 从空话具体化"。面试 talking point 拉到 §3.6(可讲 2-3 分钟"我对微信生态边界 + 角色职能落位的判断")。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析 + INTERVIEW_NOTES.md 未建**。
- **2026-05-20**:**PRD §1 补「端架构」子小节 + 分流 SVG**(`part_time_wechat_mini_program_project_mvp.html` 新 h3 `sec-1-tier` + 新 `diagram-tier-architecture.svg`)——用户在会话里问"针对三个人群岂不是要做三个小程序",触发显式补齐 PRD 原本隐含的端架构决策(L168/L209 已默认"一个小程序"但从未写出"为什么")。沿用 §3 三段式 callout 风格(决策/取舍/实现位置,class `callout-decision` 与 §3.1–§3.5 一致),三条 MVP 反向选(C/B 不拆)的理由落到**用户场景**:① 招聘方=小商户无 PC 后台习惯 ② 中介方=平台预入驻方、量级可承载 ③ 应聘方=学生在微信生态;代价(B 端批量管理受限)明示推 v2 工作台,与 §2 延期项「多招募令并发管理工作台」承接。SVG 720×320(微信用户→登录身份识别→三方主界面 tabBar),配色复用 §1 role-grid 与 §4 时序图四色(应聘 #dbeafe/#3b82f6 · 招聘 #d1fae5/#059669 · 中介 #fed7aa/#ea580c · 平台 #eeedfe/#7c3aed);marker id `tier-arr` 全仓唯一不与现有 `arr`/`arrd`/`arro` 冲突;新锚点 `sec-1-tier` 全仓零冲突。插入位置在 §1 末尾「核心机制:双重确认闭环」h3 之后、§2 `<h2 id="sec-2">` 之前,**不破坏 §1/§2/§3/§5/§6/§8 任何编号或锚点**(CLAUDE.md 编辑禁区铁律)。SVG 双份同步(PRD 内嵌 + `diagram-tier-architecture.svg`,仅差独立版有白色背景 rect,沿用 `diagram-1-flow.svg` 等现有约定)。本节零 AI 内容(端架构与 AI 无关),不需「v2·本版不做」标记。新增面试 talking point:"为什么不拆三个端"——直接拉到 §1 端架构小节回答,把"看似简单的端架构问题"转成一个能讲 2 分钟的产品判断。**不顶替 P1-1 竞品分析,下一步仍为 P1-1**。
- **2026-05-19**:**三方底部 tabBar 图标 21→24px(两文件:`full-flow-prototype.html` + `full-flow-prototype_simple_version.html`)**——用户觉得图标偏小要"放大点";三选一(24/26/28px)用户拍板 **24px**(约 +14%,微调)。只改两处尺寸:`tabIcon` 的 `e('svg',{width:21,height:21,...})`→`width:24,height:24`(`viewBox '0 0 24 24'` 不变,设 24×24 后与 viewBox 1:1 最清晰、路径自动按比例);`TabBar` 图标容器 `div` `width:21,height:21`→`24,24`(其余样式不动)。两处必须同改(容器负责居中限位)。标签字号 9px / 栏 `padding:9px 0 22px` / `gap:4` / 颜色 token / 选中(实心咖啡)未选(描边灰)逻辑 / `tabIcon` 路径数据 / `IOSDevice` 全不碰(延续上轮评审"不镀金"纪律)。已核实仅这 2 文件含该 tabBar 且目标两行字节级一致(`feedback_chrome_polish_is_intentional` + `project_full_flow_two_files_lockstep`),同两处 Edit 逐字相同 = 4 次 Edit。自检 PASS:无 JSX、仅两个 `21→24`、未选/选中逻辑零变更、`casting-prototype-standalone.html`/`__om-edit-overrides`/PRD 编号未碰、零 AI;`git diff --stat` 仅 3 文件(2 原型 + 本路线图)、两原型本次改动 byte-identical。属外壳保真延续。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-19**:**三方底部 tabBar 选中态图标填实(两文件:`full-flow-prototype.html` + `full-flow-prototype_simple_version.html`)**——用户指定选中态要更明确。三选一(红色高亮+实心 / 亮色药丸底 / 保留咖啡色+图标填实)用户拍板 **保留咖啡色 + 图标填实**:选中仍 `t.coffee`、未选仍 `t.muted`、标签色不变,**唯一变化=选中态图标由描边→实心**;不引入社论红/药丸底/反白/动画(均被用户选项排除或属镀金)。`tabIcon(k,c)`→`tabIcon(k,c,filled)`:`filled` 假分支与原描边逐字一致(`filled &&` 前置短路,未选中态像素级零变更);真分支为 6 个图标**手画干净闭合实心字形**(公文包/日历/剪贴板=实心 rect + 纸色 `t.bg` 挖空腰线/表头/勾;喇叭锥体本就 `z` 闭合;房子重画为实心轮廓 `M12 3.6L20.5 11H18.5V20H5.5V11H3.5Z`+纸色门;人像实心头+胸)——避开"naive 给开放路径设 fill 会自动闭合显丑"(用户已接受该 caveat,但工程上规避之,零额外风险)。`TabBar` 调用 `tabIcon(tb.k,col,on)`,其余结构/间距/`background:t.bg`/`borderTop`/`TABS` 全不动。已核实仅这 2 文件含 `tabIcon`/`TABS` 且该区域字节级一致(`feedback_chrome_polish_is_intentional` + `project_full_flow_two_files_lockstep`),同两处 Edit 逐字相同。自检 PASS:无 JSX、未选中态零变更、颜色 token/主题/`IOSDevice`/`casting-prototype-standalone.html`/`__om-edit-overrides`/PRD 编号未碰、零 AI;`git diff --stat` 仅 3 文件(2 原型 + 本路线图)、两原型新代码 byte-identical。属外壳保真延续,微抬 `AI_PM_DIAGNOSIS.md` §1.3。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-19**:**招聘方招募令「详情 + 修改后重新提交审核」闭环(两文件:`full-flow-prototype.html` + `full-flow-prototype_simple_version.html`)**——用户指定:招聘方「我发布的招募令」列表点卡片进详情、可改内容并重新提交审核。先核实 simple_version 是 full-flow 的 `cp`,`EmployerRecruitList`/`window` 导出/`InteractivePhone` 路由三处区域**字节级一致**,故同四处 Edit 对两文件逐字相同(diff 交叉校验:新代码两文件 byte-identical,各 +141/-30 对称)。改动:① `L`→IIFE 级 `RLIST`(4 条补 `cat/time/place/payFull/tags/desc`;**`RLIST[0]` 与 #9 `EmployerPublish`/#19a `ApplicantConfirm`/#18 `EmployerRoster`/T11 `EmployerComplete` 同一笔 ZARA 招募令字段对齐**,防跨屏字段对不上),`EmployerRecruitList(props)` 卡片可点 + `详情/修改 ›`;② 新增 `EmployerRecruitDetail`(`view→edit→reviewing→view` 状态机,镜像 `EmployerPublish` draft→reviewing→published;复用既有 `Title/Field/Primary/Ghost/paper/page` helper 零新搭骨架)+ 新增 `DecisionNote`;③ 路由:`erecruit` 传 `onOpen` push `recruitdetail`、新增分支、`hideTab` 加 `recruitdetail`(钻取子页去 tabBar、‹ 返回走 pop 回列表)、`window` 导出补 `EmployerRecruitDetail`。**两处用户取舍(AskUserQuestion 拍板)**:(a) 弃「右下角悬浮 FAB」改**底部全宽 `✎ 修改招募令` Primary**——FAB 是 Material 语言、与 cream 卷宗违和且挂 `overflow:auto` 容器会随正文滚动遮挡描述,改后与 `EmployerPublish`/`ApplicantJobDetail` 全原型一致(连带消掉 `position:relative`/占位补丁);(b) 修改屏加一条 **§3 风格 `DecisionNote`**「已有 N 人报名时改报酬/时段/类别等实质字段对已报名者契约影响、本版简化、留待 v2、参 PRD §5」——把 CRUD 升级为面试可讲的边界感素材(本仓 §3/附录 B 一贯手法),工程量仅一段静态文案。编辑禁区/铁律自检 PASS:无 JSX(纯 `React.createElement`)、`window` 桥完整、全中文无英文 placeholder(受控 `value`)、`casting-prototype-standalone.html`/`IOSDevice` 5 文件/`__om-edit-overrides` 未碰、PRD 编号未涉、`git diff --stat` 仅这 2 个文件变更;**`DecisionNote` 不复用 `AiBanner`、零 AI 文案**(此为人工+平台重审闭环,遵 memory `feedback_ai_is_future_vision`),延续 `feedback_match_cream_design`。把招聘方一侧从「几个孤立单步」补成「列表→详情→修改→重审→生效」可钻取有状态回路,继续抬升 `AI_PM_DIAGNOSIS.md` §1.3「设计完整度」(静态原型 caveat 仍诚实保留)。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-19**:**新增 `full-flow-prototype_simple_version.html`(三方首页单屏对比版 + 横竖排切换)**——用户指定:`cp` `full-flow-prototype.html` 后,三方 `Section` 的 `steps` 各压成 1 条、指向该角色 TabBar 首页 view(应聘 `browse`/职位、招聘 `erecruit`/招聘、平台 `list`/首页,依 `TABS` 行 2938–2940 核实),右上角加 `position:fixed` cream 风格「竖排/横排」切换器(`React.useState` 控 `flexDirection`,默认竖排同原版),`Section` 外层 `width:'100%'→'auto'` 防横排溢出。复用既有组件零新写,**原 `full-flow-prototype.html` 未碰**;作品集展示用(一眼看全三端入口形态),非新功能。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-19**:**同步应聘方履约闭环(月历日程 + 到岗打卡屏)**——用户在桌面 `prototype.html` 上迭代了新版,要求把仓库 `full-flow-prototype.html` 缺的补上(范围=仅此一文件)。先 `git diff --no-index` 确认两份是同一原型的两个迭代、仓库版**无任何**桌面版缺失的独立编辑,故"补缺"边界清晰。补入 5 块:① **Tweaks 系统 script 块**(`window.__TWEAKS`/`setTweaks`/`useTweaks`/`TweaksPanel`,`TweaksPanel` 返回 `null` 不显示)——**必需**,新版 `ApplicantSchedule` 调 `window.useTweaks()`,缺它整屏报错;② **`ApplicantSchedule` 重写**:静态 3 条列表 → 完整月历(`buildGrid` 6×7 网格 / 月份切换 / 有班打点 / 选日看当天班次卡片 / 浮动月历下拉);③ **新增 `ApplicantCheckIn`**:到岗打卡屏,状态机 `未到岗→已到岗→已完工` + 履约时间线(结算口径对齐 PRD T11 7 天窗口);④ **`EntryRow` 新增 `accent` 高亮**,`ApplicantMine`/`EmployerMine` 两快捷入口改高亮并删其上 `ListHint('快捷入口')`;⑤ **路由接线**:`window` 导出加 `ApplicantCheckIn`、`schedule` 传 `onOpen`、新增 `checkin` 分支、`hideTab` 加 `checkin`、`App` 末尾渲染 `TweaksPanel`。**按用户明确决定不补**末尾 `<style id="__om-edit-overrides">`(宿主可视化编辑器残留:12 条互相覆盖、绑脆弱 nth-child 的 padding/font `!important`,CLAUDE.md 标"宿主生成的,不要动")。实现用确定性单步(取桌面版 1–3138 行恰停在 `</html>` 回写,而非在 392 字符长行近压缩 HTML 上做 5 处脆弱 Edit);两文件均 UTF-8/LF/无 BOM。双向 diff 自检通过:vs 桌面版仅差被省略的 override 块、vs HEAD 恰为上述 5 块(228 ins/30 del = 243−15),无 PRD 编号破坏、`casting-prototype-standalone.html` 未碰、无 JSX 泄漏、`window` 桥完整,延续 memory `feedback_match_cream_design`/`feedback_ai_is_future_vision`(履约屏 MVP 全人工无 AI)。把应聘方一侧从"能报名"延伸到"能履约打卡结算",继续抬升 `AI_PM_DIAGNOSIS.md` §1.3「设计完整度」(静态原型 caveat 仍诚实保留)。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-18**:**门面 + 诚实止血(零代码改动)**——按用户指令再次审查仓库找问题。三路只读审查:**编辑禁区 / AI 铁律全 PASS**(IOSDevice 5 文件字节级一致、`casting-prototype-standalone.html`/`__om-edit-overrides` 未碰、无 JSX 泄漏、`window` 桥完整、PRD 编号未破、SVG 双份同步;全仓 AI 提及均带 `v2·本版不做`,零违规——近期大量微信壳改动技术上干净)。真正问题在作品集层、且不在原诊断/路线图内,本轮修三处:① **重写 `README.md`**(原仅 2 行)为对外作品集落地页(谁/这是什么/AI 在哪 v2/怎么看,仅导向对外资产,不暴露诊断·路线图);② **`AI_PM_DIAGNOSIS.md` 去过期+修死链**(§1.3 设计完整度 ★★→★★★ 封顶并保留"静态原型/平台后台仍薄"诚实 caveat;§二总结表刷新;§三被动应对死链修正:`AI_MATCHING_SPEC.md`/`METRICS.md`/原型由"待写/待画"改为已交付,`COMPETITIVE_ANALYSIS.md` 诚实保留"待补";§1.5 空洞→半空洞、§一.1 弱项、§一.2 哑火按 P0 已完成据实更新);③ **本路线图总览**把无条件"可以开始投简历"改为带条件诚实表述、显式标 `INTERVIEW_NOTES.md` 未建。按用户明确决定:chrome 打磨有原因继续,**不**记为新短板、**不**建议冻结。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-18**:**full-flow 加按角色微信底部 tabBar + 6 个净新 tab 屏**(用户指定,深度=功能简洁版、范围=仅 `full-flow-prototype.html`)。把"像真微信小程序"主线补上最后一块标志结构:`IOSDevice` 加可选 `tabBar` 形参 + home 指示条前 gated 渲染(z55,5 文件字节级一致,其余 4 文件不传→无变化,延续 memory `feedback_match_cream_design` 外壳不变量);`App`/`Section`/`Flow`/`InteractivePhone` 透传 `role`;新增 `TabBar`+`tabIcon`+`TABS`(应聘 职位/日程/我的 · 招聘 招聘/我的 · 平台 待办/我的;tab key=view,点按 `setStack` 切屏,当前屏为 tab 根时 t.coffee 高亮);职位复用 `ApplicantBrowse`,其余 6 个净新屏(`ApplicantSchedule/ApplicantMine/EmployerRecruitList/EmployerMine/PlatformTodo/PlatformMine`)复用现有屏 IIFE helper(`Title/page/paper/Avatar/Field/Stats/OrderRow`),对齐 `AgentPool` 信息密度、假数据、全中文、**MVP 全人工无 AI**(无 AI 落点不加 AI 文案,贯彻 memory `feedback_ai_is_future_vision`);`PlatformTodo` 按截止时间升序。不重构既有 journey(PRD #编号承载性,CLAUDE.md 编辑禁区);`casting-prototype-standalone.html` / `__om-edit-overrides` 未碰。继续抬升 `AI_PM_DIAGNOSIS.md` §1.3「设计完整度」。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。 *导航微调(同日,用户精确指定):平台 `TABS` 最左加「首页」tab(复用 `list` 钻取链视图,新增房子 `tabIcon`),中介方三 tab = [首页|待办|我的];#19a/#20-22/#18/T11/#9 五个 journey 任务屏去掉 tabBar、改加左上「‹」返回——confirm/deal→「我的」(cmine)、roster/complete→「我的」(emine)、publish→「招聘」(erecruit);仅 `InteractivePhone` 一处 `backTo`/`noTab` 门控,其余 view 维持常驻 tabBar。随后用户追加:报名者列表/模特档案/报名池#17(`applicants`/`detail`/`pool`)也去 tabBar(消除其自带底部操作栏被遮挡),三者经 push 进入故返回仍走 `pop`(`hideTab = noTab || applicants || detail || pool`)。再追加:用户指出应聘方缺职位详情页(原 职位列表→直接报名表单),新增 `ApplicantJobDetail`(简洁版:岗位/招聘方/时段/地点/报酬/人数/要求标签/职位描述 + 报名按钮,复用现有 IIFE helper、假数据、全中文、无 AI),流程改 `browse→jobdetail→apply`,`jobdetail`(及随后 `apply` 报名表单)并入 `hideTab`(钻取子页无 tabBar、‹ 返回走 pop)。再追加:招聘方「招聘」页(`erecruit`)顶部最左侧(与返回键同位 `left:14,top:58`;erecruit 为 tab 根栈深 1 无返回键,不冲突)加「+」新建招募令——`IOSDevice` 加可选形参 `onAdd`(同 `onBack`/`tabBar` gated 模式,5 文件字节级一致,其余 4 文件不传→零变化),`Phone` 透传,仅 `v==='erecruit'` 传 `onAdd=()=>push({view:'publish'})`,复用既有 #9 EmployerPublish 与 `publish` 的 `noTab` 返回逻辑(无新建屏、不改 backTo/hideTab)。IOSDevice / VC 字节级复用组件未碰(IOSDevice 改动按字节级同步 5 文件)。再追加(用户:所有返回逻辑都要有进入入口):confirm/deal/roster/complete 四屏有返回键但无入口,在其返回目标页 ApplicantMine(→confirm/deal)/EmployerMine(→roster/complete)新增「快捷入口」行(新 `EntryRow` helper,InteractivePhone 给 cmine/emine 传 `onOpen=push`),导航成 我的→该屏→‹回我的 闭环;publish 入口已由招聘页「+」满足;纯增量,未改 backTo/hideTab/IOSDevice/字节级复用组件。*
- **2026-05-18**:**微信小程序导航带保真(状态栏主题色 + 胶囊固定带 + 正文滚动遮盖)**——承接同日「胶囊补丁」用户实测反馈:① 状态栏区原透明、露出设备根灰 `#F2F2F7`,改为**与页面主题同色的不透明带**;② 胶囊原 `top:56` 与正文重叠,改为小程序顶部固定导航带、正文整体下移到带下方;③ 占位条原在 `overflow:auto` 内随内容滚走、上滑时正文从透明状态栏透出,改为**不透明 z10 固定带遮盖滚动内容**。实现:`IOSDevice` 加 `bg` 形参(`bg || (dark?…)` 回退,字节级一致);状态栏绝对容器加 `height:100`(56 状态栏 + 44 微信导航区)+ 不透明主题背景;`WeChatCapsule` `top:56→62`(导航区内垂直居中);各 `Phone` 调用点传 `bg` 主题 hex(cream/dual-confirm/full-flow=`#f0e8db` VC_PAPER、vogue=`#fafaf8` VA、editorial=`#ffffff` VB)且占位 `56→100`。未增删 `IOSDevice` 根子节点 → `cream-dossier`/`dual-confirm` 的宿主 `__om-edit-overrides` nth-child 不受影响;`casting-prototype-standalone.html` 仍按编辑禁区排除。继续抬升 `AI_PM_DIAGNOSIS.md` §1.3「设计完整度」。微调:胶囊与状态栏图标视觉间距按用户指定从 ~14px 缩到 10px(`WeChatCapsule` `top:62→58`,图标底 ≈y48 → 间距 10px;5 文件字节级一处,导航带高/底距不变)。**返回键归位**(用户指定,全部 4 个有返回的文件):返回控件从滚动正文(full-flow 的 `BackBar` 条、cream/editorial/vogue 各屏内嵌「← 返 回/Back/BACK」)移入导航带左上,改为微信极简「‹」(细 chevron,丢 tag),`left:14` 镜像右上胶囊 `right:14`、同 `top:58`、`zIndex:40`;`IOSDevice` 加 `onBack` 形参经 `Phone`/`InteractivePhone` 透传,`stack.length>1`(非 root)才显示;原屏内按钮替换为空 `<span>`(保留 space-between 行、右侧 issueNo/卷宗标签不动);full-flow 的 `BackBar` 仅停用不删;dual-confirm 无导航不受影响,`IOSDevice` 仍字节级一致。继续抬升 §1.3「设计完整度」。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-18**:**微信小程序外壳保真补丁**——按用户指令,给 5 个可点击原型(`model_selection_business/cream-dossier.html`、`editorial-grid.html`、`vogue-fashion-magazine.html`、`dual-confirm-prototype.html`、`part_time_job_wechat_mini_program_prototype/full-flow-prototype.html`)的 `IOSDevice` 外壳统一新增 **`WeChatCapsule` 微信小程序胶囊按钮**(状态栏正下方右上角:`•••` + 分隔线 + 同心圆,磨砂半透明,随既有 `dark` 标志在黑/白间适配,`pointerEvents:'none'` 不挡原型交互;作为设备级 chrome 出现一次、浮于所有屏之上)。三处插入(组件定义 / `IOSDevice` 内渲染 / `Object.assign` 导出)在 5 文件中**字节级相同**(memory `feedback_match_cream_design`),`casting-prototype-standalone.html` 按 `CLAUDE.md` 编辑禁区排除。直接消除"这其实是通用 iOS 壳、不是真微信小程序"的可被当场戳穿的破绽,抬升 `AI_PM_DIAGNOSIS.md` §1.3「设计完整度」。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-18**:**扩展 P1-2**——按用户指令新建 `part_time_job_wechat_mini_program_prototype/full-flow-prototype.html`,把原型从"#18/#19a 两屏"扩为**应聘 / 招聘 / 中介三端关键决策节点的一条可点击流**(单文件、按角色分段、段内左→右):应聘方 #11 收到匹配推送→#14 报名→#19a 确认接受→成单/完工;招聘方 #9 发布招募令→#18 确认名单(复用)→T11 完工确认;中介方 报名池→#17 提交有序 rank。直接抬升 `AI_PM_DIAGNOSIS.md` §1.3「只覆盖 1/4 视角」短板。实现上 `cp` dual-confirm 后只改页眉 + 增 6 屏 + 重写装配,**iOS frame / VC token 字节级复用**(memory `feedback_match_cream_design`);#18/#19a 两屏原样保留。AI 严格按铁律(memory `feedback_ai_is_future_vision`):仅在应聘 #11 推送、招聘 #9 字段说明、中介 #17 报名池三个自然落点出现,每处带醒目「v2 · 本版不做」+ 页眉全局 AI 说明,MVP 全人工。**未改 P0/P1 排序,下一步仍为 P1-1 竞品分析**。
- **2026-05-17**:完成 **P0-2**——新建 `METRICS.md`,定位为**指标设计/验证假设体系**(非数据报告:产品未上线、零实测数据,所有阈值标注"假设·待验证",正面回应"没数据能不能做指标"——能,AI PM 考的是"会不会用数据思考"而非"有没有数据")。北极星 = **双确认成单率**(发布→已成单且 7 日内未取消,"7 日尾巴"抗刷量,同时覆盖 §3.1+§3.2+§3.4);§3.1–§3.4 逐条配验证指标 + 假设阈值 + 数据来源;含三条护栏指标与 §4「证伪逻辑」(给出"什么结果出现→推翻哪条假设→下一步怎么改",直接对应 `AI_PM_DIAGNOSIS.md` §1.1 短板与面试高频题)。同步更新本路线图总览(P0 两项 + P1-2 已完成,**可以开始投简历**);埋点字段级规格仍归 P1-3 `TRACKING_SPEC.md`。另:用户问"做微信小程序最合适的语言"——结论是**不切真·小程序栈**,继续用浏览器可开 React 原型(可甩链接、ROI 高),真小程序对 AI PM 边际增益≈0,详见会话计划。
- **2026-05-16**:P0-1 定位为 **v2 愿景(非 MVP)**——v1 小程序未起步,大模型接入延后。交付 `AI_MATCHING_SPEC.md`(顶标 v2/非 MVP)、PRD 新增 §6.3「匹配引擎口径说明」、§8 v2 表后指引段。**先**砍掉 §3.5,**后按用户同日第二次明确指令重新新增 PRD §3.5**「AI 辅助筛选」——三段式 callout,标题带 `v2 · 本版不做` 徽章 + 开头加粗范围标记,放在最被精读的 §3 以最大化 AI 含量可见性,同时确保不被误读为 MVP 已实现。同日提前完成 **P1-2**:新建 `model_selection_business/dual-confirm-prototype.html`,含 #19a 应聘方确认接受、#18 招聘方确认名单两屏(后者把 §3.4「系统不会替你默认通过」做进 UI;候选卡标注"v2 此处将由 AI 给理由",顺带可视化 AI 愿景但不假装 MVP 有 AI)。

- **2026-05-20(PRD §4 加列 + 新增 §4.5 三方端到端叙事)**:把 `part_time_job_wechat_mini_program_prototype/full-flow-prototype_simple_version.html` 这条"聚焦版"原型与 PRD 的对应关系**集中显式化**——① §4 关键消息说明表新增第 4 列「原型屏(见 simple_version)」,9 行各填**屏顶可见大标题 / 主页入口名 / 按钮文案**(如 <code>「报名池 · POOL · 排序并提交」</code> <code>「24h 倒计时确认屏」</code> <code>「确认入选名单」</code>),#19a 行带 `★ NEW` 高亮;② §4 与 §5 之间新增 §4.5「一条招募令的三方端到端叙事(基于 simple_version 原型)」,分应聘方 / 招聘方 / 中介方三视角,每步格式 `动作 — §4 消息# — §5 状态迁移 — 屏名`,末尾 4.5.4 给 10 行三方衔接映射表(原型屏 ↔ §4 消息 ↔ §5 状态 ↔ 主要角色 ↔ 关联设计决策)。**两项铁律**:(a) 屏名一律用原型查看者肉眼能对上的可见字样(用户当面拒过源码组件名 `EmployerPublish` 等;新 memory `feedback_doc_uses_visible_labels` 已存);(b) §4.5 全程不出现 AI / 模型 / 算法 / 推荐 / 置信度字样,AI 解释职责保留在 §3.5 + AI_MATCHING_SPEC.md(即便原型 5 处带 v2 AI banner,§4.5 走过这些位置只字不提)。**零改动**:消息编号 #1–#22 / 状态名 / 超时 T1–T11 / SVG / 附录 A 三向索引 全部 0 变更。归类:**P1-2 后续延伸**(P1-2 早已 ✅),不计新 P 项。
